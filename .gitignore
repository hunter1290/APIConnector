# === OS ===
.DS_Store
Thumbs.db

# === IDEs / editors ===
.idea/
.vscode/
*.iml
*.swp

# === Java / Maven (backend also has its own .gitignore) ===
target/
!.mvn/wrapper/maven-wrapper.jar

# === Node / Next.js (frontend also has its own .gitignore) ===
node_modules/
.next/
out/

# === Environment files (never commit secrets) ===
.env
.env.local
.env.*.local
